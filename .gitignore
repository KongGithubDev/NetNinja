# Dependencies
node_modules/

# Environment
.env

# Secrets - NEVER commit
azure-sg.key
*.key

# Go binary
*.exe

# OS files
.DS_Store
Thumbs.db

# Certificates
fullchain.pem
privkey.pem

# Build artifacts
dist/
proxy_linux*
netninja-proxy-linux
netninja-keepalive-linux

# Test scripts
test_*.ps1

# Local-only docs (kept on disk, never published)
TROUBLESHOOTING.md

# Per-deployment server data (uploaded by the deploy scripts, not shared)
geo-nodes.txt
geo-domains.txt

# Thai pool supervisor config — holds the real tunnel endpoints/commands
th-pool.conf
netninja-th-pool.conf

# Local deploy settings — the real server host/user lives here, never in a
# tracked script (the repository is public)
netninja.local.ps1
*.local.ps1
netninja.local.sh
*.local.sh

# Runtime data
proxy_cache.db
proxy_cache.db-shm
proxy_cache.db-wal
__pycache__/
*.pyc